name: 🚀 Release Plugin

on:
  push:
    tags:
      - 'v*'

jobs:
  # ============================================================================
  # BUILD PLUGIN PACKAGE
  # ============================================================================
  build:
    name: Build Plugin Package
    runs-on: ubuntu-latest
    outputs:
      plugin_slug: ${{ steps.info.outputs.slug }}
      plugin_name: ${{ steps.info.outputs.name }}
      plugin_version: ${{ steps.info.outputs.version }}
      package_name: ${{ steps.package.outputs.name }}
      changelog_content: ${{ steps.changelog.outputs.content }}

    steps:
      - name: Checkout plugin source
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Extract plugin information
        id: info
        run: |
          # Extract tag version without 'v' prefix
          TAG_VERSION="${GITHUB_REF_NAME#v}"

          # Auto-detect plugin slug and file from repository name
          REPO_NAME="${GITHUB_REPOSITORY##*/}"
          PLUGIN_SLUG="${REPO_NAME}"
          PLUGIN_FILE="${PLUGIN_SLUG}.php"

          # Verify plugin file exists, otherwise find it
          if [ ! -f "$PLUGIN_FILE" ]; then
            PLUGIN_FILE=$(find . -maxdepth 1 -name "*.php" -exec grep -l "Plugin Name:" {} \; | head -1)
            PLUGIN_SLUG=$(basename "$PLUGIN_FILE" .php)
          fi

          PLUGIN_NAME=$(grep "Plugin Name:" "$PLUGIN_FILE" | head -1 | sed 's/.*Plugin Name:\s*\([^*]*\).*/\1/' | xargs)

          echo "slug=${PLUGIN_SLUG}" >> $GITHUB_OUTPUT
          echo "name=${PLUGIN_NAME}" >> $GITHUB_OUTPUT
          echo "version=${TAG_VERSION}" >> $GITHUB_OUTPUT

          echo "📦 Plugin: ${PLUGIN_NAME} (${PLUGIN_SLUG})"
          echo "🏷️ Version: ${TAG_VERSION}"
          echo "🔖 Tag: ${GITHUB_REF_NAME}"

      - name: Extract changelog content
        id: changelog
        run: |
          VERSION="${{ steps.info.outputs.version }}"

          echo "Extracting changelog for version: ${VERSION}"

          # Extract changelog from CHANGELOG.md if it exists
          if [ -f "CHANGELOG.md" ]; then
            CHANGELOG=$(sed -n "/^## \[${VERSION}\]/,/^## /p" CHANGELOG.md | sed '1d;$d')
            if [ -z "$CHANGELOG" ]; then
              CHANGELOG=$(sed -n "/^## Unreleased/,/^## /p" CHANGELOG.md | sed '1d;$d')
            fi
          else
            CHANGELOG="Release ${VERSION}"
          fi

          {
            echo 'content<<EOF'
            echo "$CHANGELOG"
            echo 'EOF'
          } >> $GITHUB_OUTPUT

      - name: Create release package
        id: package
        run: |
          PLUGIN_SLUG="${{ steps.info.outputs.slug }}"
          PLUGIN_VERSION="${{ steps.info.outputs.version }}"
          PACKAGE_NAME="${PLUGIN_SLUG}_v${PLUGIN_VERSION}.zip"

          echo "📦 Creating package: $PACKAGE_NAME"

          # Create ZIP package excluding development files
          zip -r "$PACKAGE_NAME" . \
            -x "*.git*" \
            -x "*.github*" \
            -x "node_modules/*" \
            -x "*.DS_Store*" \
            -x "*.log" \
            -x "*.md" \
            -x "*.zip"

          # Verify package was created
          if [ -f "$PACKAGE_NAME" ]; then
            FILE_SIZE=$(ls -lh "$PACKAGE_NAME" | awk '{print $5}')
            sha256sum "$PACKAGE_NAME" > "${PACKAGE_NAME}.sha256"

            echo "✅ Package created: $PACKAGE_NAME ($FILE_SIZE)"
            echo "name=${PACKAGE_NAME}" >> $GITHUB_OUTPUT
          else
            echo "❌ Failed to create package"
            exit 1
          fi

      - name: Upload package artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.info.outputs.slug }}-${{ steps.info.outputs.version }}
          path: |
            ${{ steps.package.outputs.name }}
            ${{ steps.package.outputs.name }}.sha256
          retention-days: 30

  # ============================================================================
  # CREATE GITHUB RELEASE
  # ============================================================================
  github-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: build
    outputs:
      release_url: ${{ steps.set_url.outputs.release_url }}
      upload_url: ${{ steps.release.outputs.upload_url }}

    steps:
      - name: Download package artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.build.outputs.plugin_slug }}-${{ needs.build.outputs.plugin_version }}

      - name: Create GitHub Release
        id: release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ needs.build.outputs.plugin_name }} ${{ github.ref_name }}
          body: |
            # ${{ needs.build.outputs.plugin_name }} ${{ github.ref_name }}

            ${{ needs.build.outputs.changelog_content }}

            ## 📦 Installation

            1. Download the plugin ZIP file below
            2. Upload to WordPress via Plugins → Add New → Upload Plugin
            3. Activate the plugin

            ---

            **Package**: `${{ needs.build.outputs.package_name }}`
            **Generated**: ${{ github.sha }}
          files: |
            ${{ needs.build.outputs.package_name }}
            ${{ needs.build.outputs.package_name }}.sha256
          draft: false
          prerelease: true
          generate_release_notes: false

      - name: Set release URL output
        id: set_url
        run: |
          RELEASE_URL="https://github.com/${{ github.repository }}/releases/tag/${{ github.ref_name }}"
          echo "release_url=${RELEASE_URL}" >> $GITHUB_OUTPUT
          echo "✅ Release URL: ${RELEASE_URL}"

  # ============================================================================
  # NOTIFY EDD STORE
  # ============================================================================
  edd-webhook:
    name: Notify EDD Store
    runs-on: ubuntu-latest
    needs: [build, github-release]
    if: |
      always() &&
      needs.build.result == 'success' &&
      needs.github-release.result == 'success'

    steps:
      - name: Get release asset API URL
        id: get_asset
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          PACKAGE_NAME="${{ needs.build.outputs.package_name }}"

          echo "📦 Fetching asset ID for: $PACKAGE_NAME"

          # Get release assets from GitHub API
          ASSETS_JSON=$(gh api repos/${{ github.repository }}/releases/tags/${{ github.ref_name }} --jq '.assets')

          # Find the matching asset and extract its API URL
          ASSET_URL=$(echo "$ASSETS_JSON" | jq -r ".[] | select(.name == \"$PACKAGE_NAME\") | .url")

          if [ -z "$ASSET_URL" ] || [ "$ASSET_URL" == "null" ]; then
            echo "❌ Could not find asset URL for $PACKAGE_NAME"
            exit 1
          fi

          echo "asset_url=${ASSET_URL}" >> $GITHUB_OUTPUT
          echo "✅ Asset API URL: ${ASSET_URL}"

      - name: Send EDD webhook notification
        env:
          EDD_WEBHOOK_URL: ${{ secrets.EDD_WEBHOOK_URL }}
          EDD_WEBHOOK_TOKEN: ${{ secrets.EDD_WEBHOOK_TOKEN }}
          EDD_PRODUCT_ID: ${{ secrets.EDD_PRODUCT_ID }}
        run: |
          PLUGIN_SLUG="${{ needs.build.outputs.plugin_slug }}"
          PLUGIN_VERSION="${{ needs.build.outputs.plugin_version }}"
          RELEASE_URL="${{ needs.github-release.outputs.release_url }}"
          ASSET_API_URL="${{ steps.get_asset.outputs.asset_url }}"
          DOWNLOAD_URL="https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}/${{ needs.build.outputs.package_name }}"

          echo "📡 Sending webhook to EDD Store..."
          echo "Plugin: $PLUGIN_SLUG"
          echo "Version: $PLUGIN_VERSION"
          echo "Release URL: $RELEASE_URL"
          echo "Asset API URL: $ASSET_API_URL"
          echo "Download URL: $DOWNLOAD_URL"
          echo "Note: readme_url auto-generated from release_url"

          # Check for required secrets
          if [ -z "$EDD_WEBHOOK_URL" ]; then
            echo "⚠️ EDD_WEBHOOK_URL secret not configured - skipped"
            exit 0
          fi

          if [ -z "$EDD_PRODUCT_ID" ]; then
            echo "⚠️ EDD_PRODUCT_ID secret not configured - skipped"
            exit 0
          fi

          # Send webhook payload to EDD Release Manager
          # EDD_PRODUCT_ID must be set as repository secret
          # asset_api_url is the GitHub API URL for Git Updater dropdown compatibility
          # download_url is the browser download URL for backward compatibility
          # readme_url is optional - auto-generated by EDD Release Manager
          curl -X POST "$EDD_WEBHOOK_URL" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer $EDD_WEBHOOK_TOKEN" \
            -d '{
              "edd_id": '$EDD_PRODUCT_ID',
              "plugin": "'$PLUGIN_SLUG'",
              "version": "'$PLUGIN_VERSION'",
              "release_url": "'$RELEASE_URL'",
              "asset_api_url": "'$ASSET_API_URL'",
              "download_url": "'$DOWNLOAD_URL'",
              "github_tag": "${{ github.ref_name }}",
              "repository": "${{ github.repository }}",
              "package_name": "${{ needs.build.outputs.package_name }}",
              "sha": "${{ github.sha }}"
            }' \
            --fail --silent --show-error

          echo "✅ EDD webhook sent successfully"

  # ============================================================================
  # SLACK NOTIFICATION
  # ============================================================================
  slack-notification:
    name: Slack Notification
    runs-on: ubuntu-latest
    needs: [build, github-release, edd-webhook]
    if: always() && needs.build.result == 'success'

    steps:
      - name: Send Slack notification
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          PLUGIN_NAME="${{ needs.build.outputs.plugin_name }}"
          PLUGIN_VERSION="${{ needs.build.outputs.plugin_version }}"
          RELEASE_URL="${{ needs.github-release.outputs.release_url }}"

          # Build status indicators
          GITHUB_STATUS="${{ needs.github-release.result == 'success' && '✅ Created' || '❌ Failed' }}"
          EDD_STATUS="${{ needs.edd-webhook.result == 'success' && '✅ Synced' || needs.edd-webhook.result == 'skipped' && '⏭️ Skipped' || '❌ Failed' }}"

          TITLE="🚀 RELEASE: ${PLUGIN_NAME} ${{ github.ref_name }}"

          # Build message text
          MESSAGE="*Release Summary for ${PLUGIN_NAME}*\n"
          MESSAGE="${MESSAGE}• *Version:* ${{ github.ref_name }} (${PLUGIN_VERSION})\n"
          MESSAGE="${MESSAGE}• *GitHub Release:* ${GITHUB_STATUS}\n"
          MESSAGE="${MESSAGE}• *EDD Webhook:* ${EDD_STATUS}\n\n"
          MESSAGE="${MESSAGE}📦 <${RELEASE_URL}|Download Release>\n\n"
          MESSAGE="${MESSAGE}#release #automation #popup-maker"

          echo "📢 Sending Slack notification..."
          echo "Title: $TITLE"

          if [ -z "$SLACK_WEBHOOK_URL" ]; then
            echo "⚠️ SLACK_WEBHOOK_URL secret not configured - skipped"
            exit 0
          fi

          # Send to Slack
          curl -X POST \
            --data-urlencode "payload={\"text\": \"${TITLE}\\n\\n${MESSAGE}\", \"username\": \"Release Bot\", \"icon_emoji\": \":rocket:\"}" \
            "$SLACK_WEBHOOK_URL" \
            --fail --silent --show-error

          echo "✅ Slack notification sent successfully"

  # ============================================================================
  # RELEASE SUMMARY
  # ============================================================================
  summary:
    name: Generate Release Summary
    runs-on: ubuntu-latest
    needs: [build, github-release, edd-webhook, slack-notification]
    if: always()

    steps:
      - name: Generate release summary
        run: |
          PLUGIN_NAME="${{ needs.build.outputs.plugin_name }}"
          VERSION="${{ github.ref_name }}"

          echo "# 🚀 Release Summary: ${PLUGIN_NAME} ${VERSION}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          echo "**Triggered by:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          echo "## Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          [ "${{ needs.build.result }}" == "success" ] && echo "✅ **Package Creation**: Package built successfully" >> $GITHUB_STEP_SUMMARY || echo "❌ **Package Creation**: Failed" >> $GITHUB_STEP_SUMMARY
          [ "${{ needs.github-release.result }}" == "success" ] && echo "✅ **GitHub Release**: Release created" >> $GITHUB_STEP_SUMMARY || echo "❌ **GitHub Release**: Failed" >> $GITHUB_STEP_SUMMARY
          [ "${{ needs.edd-webhook.result }}" == "success" ] && echo "✅ **EDD Webhook**: Webhook delivered" >> $GITHUB_STEP_SUMMARY || echo "❌ **EDD Webhook**: Failed or skipped" >> $GITHUB_STEP_SUMMARY
          [ "${{ needs.slack-notification.result }}" == "success" ] && echo "✅ **Slack Notification**: Notification sent" >> $GITHUB_STEP_SUMMARY || echo "❌ **Slack Notification**: Failed or skipped" >> $GITHUB_STEP_SUMMARY

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📦 Package Information" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**File:** \`${{ needs.build.outputs.package_name }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Download:** [GitHub Release](${{ needs.github-release.outputs.release_url }})" >> $GITHUB_STEP_SUMMARY

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "*🚀 Release Generated by GitHub Actions*" >> $GITHUB_STEP_SUMMARY